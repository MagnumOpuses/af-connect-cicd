apiVersion: v1
kind: BuildConfig
metadata:
    labels:
        app: af-connect-pipeline
    name: af-connect-pipeline
spec:
    source:
        type: git
        git:
            ref: stage
            uri: https://github.com/MagnumOpuses/af-connect.git
    strategy:
        jenkinsPipelineStrategy:
            jenkinsfilePath: ./Jenkinsfile
        type: JenkinsPipeline
    triggers:
        - github:
              secret: secret
          type: GitHub